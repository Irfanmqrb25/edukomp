/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["plus.unsplash.com", "images.unsplash.com", "github.com", "utfs.io"],
    }
}

module.exports = nextConfig
